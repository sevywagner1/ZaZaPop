var React = require('react');
var TopBar = require('TopBar');

var Main = React.createClass({
  render: function () {
    return (
      <div>
        <TopBar/>
      </div>
    );
  }
});

module.exports = Main;
